Supabase helpers live here.

Planned split:

- `env.ts` for validated environment access
- `browser.ts` for client-side Supabase setup
- `server.ts` for server-side Supabase setup
- `admin.ts` for service-role tasks
