import { RoutePlaceholder } from "@/components/ui/route-placeholder";

export default function StaffWaitlistPage() {
  return (
    <RoutePlaceholder
      area="Staff"
      title="Waitlist"
      path="/staff/waitlist"
      description="Waitlist route for filling openings quickly when appointments are canceled or rescheduled."
    />
  );
}
