import { RoutePlaceholder } from "@/components/ui/route-placeholder";

export default function StaffNotificationsPage() {
  return (
    <RoutePlaceholder
      area="Staff"
      title="Notifications"
      path="/staff/notifications"
      description="Staff notifications center for appointment reminders, bulk messages, and operational alerts."
    />
  );
}
