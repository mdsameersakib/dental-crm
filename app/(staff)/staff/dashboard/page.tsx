import { RoutePlaceholder } from "@/components/ui/route-placeholder";

export default function StaffDashboardPage() {
  return (
    <RoutePlaceholder
      area="Staff"
      title="Dashboard"
      path="/staff/dashboard"
      description="Staff dashboard route for future clinic metrics, at-a-glance summaries, and quick actions."
    />
  );
}
