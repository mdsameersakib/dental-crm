import { RoutePlaceholder } from "@/components/ui/route-placeholder";

export default function PatientProfilePage() {
  return (
    <RoutePlaceholder
      area="Patient"
      title="Profile"
      path="/profile"
      description="Patient profile route for contact details, preferences, security settings, and support access."
    />
  );
}
